const fs = require("fs"); const main = fs.readFileSync("src/main.ts", "utf8"); fs.writeFileSync("src/main.ts", "window.onerror = function(msg, url, lineNo, columnNo, error) { alert(msg); document.body.innerHTML += '<div style=\"color:red; background:white; position:fixed; z-index:9999; top:0\">' + msg + '<br>' + error?.stack + '</div>'; return false; }; window.onunhandledrejection = function(e) { document.body.innerHTML += '<div style=\"color:red; background:white; position:fixed; z-index:9999; top:0\">' + e.reason + '</div>'; }; " + main);  
